# ── Core UI ──────────────────────────────────────
streamlit>=1.35.0

# ── Data ─────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0

# ── HTTP / AI API ────────────────────────────────
requests>=2.31.0

# ── Configuration ────────────────────────────────
PyYAML>=6.0

# ── File Extraction ──────────────────────────────
PyMuPDF>=1.23.0           # fitz：PDF 文字抽取 + 圖像渲染
python-docx>=1.1.0        # DOCX 讀取
openpyxl>=3.1.0           # XLSX 讀取
python-pptx>=0.6.23       # PPTX 讀取
Pillow>=10.0.0            # 影像處理（OCR 必要）

# ── OCR (PaddleOCR, CPU-only, Streamlit Cloud friendly) ──
paddleocr==3.5.0
paddlepaddle==3.2.0

# ── Google APIs ──────────────────────────────────
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.126.0

opencv-contrib-python==0.0.0

pytesseract>=0.3.10
