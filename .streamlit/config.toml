[theme]
base = "light"
# 明確指定顏色，避免 Streamlit 前端讀到空字串而產生
# "Invalid color passed for primaryColor in theme" 的 console 警告。
# 以下與 Streamlit 預設 Light 主題相同，外觀不會改變。
primaryColor = "#FF4B4B"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F0F2F6"
textColor = "#31333F"
